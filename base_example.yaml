# Enable Home Assistant API
api:
  encryption:
    key: "bgvAcjlkIw="

ota:
  platform: esphome
  password: "ddsljksfdjmkl

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: True

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "ESP32 RMT UART Test Fallback"
    password: "lkdjsa"

captive_portal:

web_server:
  port: 80